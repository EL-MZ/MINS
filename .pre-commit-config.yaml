repos:
  - repo: local
    hooks:
      - id: ruff-format
        name: ruff format
        entry: ruff format
        language: python
        additional_dependencies: [ruff==0.14.1]
        types_or: [python, pyi]
        stages: [pre-commit]